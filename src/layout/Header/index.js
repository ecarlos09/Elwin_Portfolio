import React from 'react';

import { Canvas, StartButton } from '../../components';

const Header = () => {
    return (
        <header>
            <Canvas />
            <StartButton />
        </header>
    );
};

export default Header;